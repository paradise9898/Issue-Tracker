doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title Admin Registration
    body 
.body-content
    .module
        h1 Create an account
        form.form(action='/adminauth' method='post')
            .alert.alert-error
            
            input#Username(type='text' placeholder='username' name='username' required='')
            input#Issue(type='text' placeholder='issue' name='issue' required='')
            
            
    
            input.btn.btn-block.btn-primary(type='submit')

script.
  Document.getElementById('Username').textContent = Username;
  Document.getElementById('Issue').textContent = Issue;
  

  const data = {Name, Surname, Email, Password};
  const options = {
  method: 'POST'
  headers: {
  'Content-Type': 'application/json'
  },
  body: JSON.stringify(data)
  };
  fetch('' , options)
  console.log(Name)
  console.log(Surname)
  console.log(Email)
  console.log(Password)

  fetch('/req' , options)


